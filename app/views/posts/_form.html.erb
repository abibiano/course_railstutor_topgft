<%= form_for(@post) do |f| %>
	<%= f.label :url %>
	<%= f.text_field :url, "URL" %>

	<%= f.label :title %>
	<%= f.text_field :title %>

	<%= f.submit "Save", class: "btn btn-large btn-primary" %>
<% end %>